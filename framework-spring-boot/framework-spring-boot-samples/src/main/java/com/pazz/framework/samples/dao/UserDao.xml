<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=" net.yto.framework.samples.dao.UserDao">
    <select id="queryCountByUserName" resultType="long">
    	select count(1) from t_bse_user where id = #{id,jdbcType=VARCHAR}
    </select>
</mapper>
